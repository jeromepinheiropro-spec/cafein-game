{
  "name": "cafein-game",
  "version": "1.0.0",
  "description": "Le Barista — le jeu d'arcade secret de Cafein",
  "scripts": {
    "start": "serve -s . -l tcp://0.0.0.0:${PORT:-3000}"
  },
  "dependencies": {
    "serve": "^14.2.3"
  },
  "engines": { "node": ">=20" }
}
